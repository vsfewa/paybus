<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pay">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.NFC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SaomaResult"></activity>
        <activity android:name=".RecordAdapter" />
        <activity android:name=".ChargeActivity" />
        <activity android:name=".ModifyActivity" />
        <activity android:name=".PersonalActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".RecordActivity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".UserInfo" />
        <activity android:name=".UserActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SearchUserOut" />
        <activity android:name=".SearchPayInfo" />
        <activity android:name=".SearchUserInfo" />
        <activity android:name=".content_item" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>